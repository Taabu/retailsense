# RetailSense Producer

Sensor data simulator for the RetailSense data pipeline.

## Overview

This module generates realistic sensor data simulating foot traffic in retail stores and publishes it to MQTT for downstream processing.

## Usage

```bash
uv run python -m retailsense_producer.main